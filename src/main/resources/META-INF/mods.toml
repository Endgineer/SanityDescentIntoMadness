modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"
issueTrackerURL="https://github.com/desynq/SanityDescentIntoMadness/issues"

[[mods]]
modId="${mod_id}"
version="${minecraft_version}-${mod_version}"

displayName="${mod_name}"

updateJSONURL="https://raw.githubusercontent.com/croissantnova/SanityDescentIntoMadness/main/update.json" # TODO: Change to fork
displayURL="https://modrinth.com/mod/sanity-descent-into-madness"
logoFile= "sanitydim512.png"

credits="${mod_credits}"
authors="${mod_authors}"

description="${mod_description}"

[[dependencies."${mod_id}"]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies."${mod_id}"]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies."${mod_id}"]]
    modId="geckolib"
    mandatory=true
    versionRange="${geckolib_version_range}"
    side="BOTH"